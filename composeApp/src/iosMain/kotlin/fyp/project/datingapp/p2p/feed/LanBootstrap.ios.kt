package fyp.project.datingapp.p2p.feed

/**
 * iOS stub. TODO(iOS): Bonjour (`NWBrowser` / `NWListener`) equivalent of the
 * Android NsdManager bootstrap. No-op until iOS leaves the lazy state.
 */
class IosLanBootstrap : LanBootstrap {
    override fun start() {}
    override fun stop() {}
}
